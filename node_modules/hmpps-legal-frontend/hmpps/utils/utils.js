"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatMiniProfileDateOfBirth = exports.formatMiniProfileName = void 0;
const date_fns_1 = require("date-fns");
const isBlank = (str) => !str || /^\s*$/.test(str);
const capitalizeFirstLetter = (word) => word.length >= 1 ? word[0].toUpperCase() + word.toLowerCase().slice(1) : word;
const capitalizeFirstLetters = (word) => isBlank(word) ? '' : word.split('-').map(capitalizeFirstLetter).join('-');
const formatMiniProfileName = (person) => {
    return `${capitalizeFirstLetters(person.lastName)}, ${capitalizeFirstLetters(person.firstName)}`;
};
exports.formatMiniProfileName = formatMiniProfileName;
const formatMiniProfileDateOfBirth = (dateOfBirth) => {
    const date = (0, date_fns_1.parse)(dateOfBirth, 'yyyy-MM-dd', new Date());
    return (0, date_fns_1.format)(date, 'dd/MM/yyyy');
};
exports.formatMiniProfileDateOfBirth = formatMiniProfileDateOfBirth;
//# sourceMappingURL=utils.js.map